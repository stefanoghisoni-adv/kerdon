.env
.env.local
node_modules/
dist/
build/
.cache/
prisma/migrations/dev.db*
*.log
.DS_Store

/generated/prisma

# Scratch SDD interno (non deve finire nel repo pubblico)
.superpowers/

# Script di sonda usa e getta: leggono .env e interrogano le API vere.
.probe.mjs
.cmp.mjs
.gq-probe.mjs
.rest-probe.mjs
.q.graphql

# L'SBOM lo genera la CI a ogni esecuzione: in repository sarebbe solo
# rumore che cambia a ogni install.
sbom.cyclonedx.json

# Playwright: i browser scaricati, i risultati e le tracce di un'esecuzione.
# Tracce, immagini e video si producono SOLO quando qualcosa fallisce, e sono
# roba di quell'esecuzione li': in repository sarebbero rumore che cambia a ogni
# giro.
test-results/
playwright-report/
blob-report/
.playwright/
